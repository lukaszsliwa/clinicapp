Hi,

Looks like you did not finish a test. To continue a test use like below:

<%= public_trial_patient_questions_url(@trial, @patient, :host => 'clinickapp.com', :page => 1, :token => @patient.token, :patient_id => @patient.id) %>
